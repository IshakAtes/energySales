name: Autodeploy Dealcheckers Page

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build Docker image
        run: |
          docker build -t ghcr.io/ishakates/energysales:latest .

      - name: Push Docker image
        run: |
          docker push ghcr.io/ishakates/energysales:latest

      - name: Deploy on server
        uses: appleboy/ssh-action@v1.1.0
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.ENERGY_WEBSITE_KEY }}
          script: |
            echo "Pulling Docker image..."
            docker pull ghcr.io/ishakates/energysales:latest
            echo "Stopping previous container if exists..."
            docker stop energysales || true
            docker rm energysales || true
            echo "Starting new container..."
            docker run -d \
              -p ${{ secrets.PORT }}:80 \
              --name energysales \
              ghcr.io/ishakates/energysales:latest
